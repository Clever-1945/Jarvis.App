<Window 
    x:Class="Jarvis.App.Settings.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Jarvis.App.Settings"
    mc:Ignorable="d"
    Title="Настройки"
    WindowStartupLocation="CenterScreen"
    Height="450"
    Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TabControl Grid.Column="0" Grid.Row="0">
            <TabItem Header="Основное">
                <local:SettingsMainControl Settings="{Binding Settings}"/>
            </TabItem>
            <TabItem Header="Плагины">
                <local:SettingsPluginsControl Settings="{Binding Settings}" />
            </TabItem>
        </TabControl>
        
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="80"></ColumnDefinition>
                <ColumnDefinition Width="80"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Button Margin="4" Grid.Column="1" Click="OnCancel">Отмена</Button>
            <Button Margin="4" Grid.Column="2" Click="OnOk">Ok</Button>
        </Grid>
    </Grid>
</Window>

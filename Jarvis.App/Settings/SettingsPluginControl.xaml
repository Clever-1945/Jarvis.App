<UserControl 
    x:Class="Jarvis.App.Settings.SettingsPluginControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Jarvis.App.Settings"
    mc:Ignorable="d"
    x:Name="ThisControl"
    d:DesignHeight="300" 
    d:DesignWidth="300">
    <Grid Name="NameGrid" Visibility="Hidden">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Margin="2" Text="{Binding ElementName=ThisControl, Path=PluginDescription.Name}"></TextBlock>
        <CheckBox Grid.Row="1" Margin="2" IsChecked="{Binding ElementName=ThisControl, Path=PluginSettings.IsDisabled}"> Отключить плагин </CheckBox>
    </Grid>
</UserControl>
